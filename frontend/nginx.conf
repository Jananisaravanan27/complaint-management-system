server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index login.html;

    location /api/ {
        proxy_pass http://complaint-backend:5000/api/;
        proxy_set_header Host $host;
    }
}